# QTQ HQ — trụ sở điều phối
**Phiên mới bắt đầu**: đọc `CLAUDE.md` (hiến pháp) → `INDEX.md` (bản đồ) → `projects/<dự-án>/STATE.md` → làm việc → kết thúc bằng skill `dong-phien`.
Cấu trúc: projects/ (trạng thái sống) · decisions/ (log quyết định) · skills/ (quy trình chung) · templates/ (prereg).
Janitor: skill `don-dep` chạy Routine tuần (xem ROUTINES.md). Migrate sang repo riêng: MIGRATE.md.
