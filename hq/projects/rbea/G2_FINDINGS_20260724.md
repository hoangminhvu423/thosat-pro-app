# G2 — KẾT QUẢ: **FAIL** (2026-07-24, cửa sổ deploy MacBook, headless tester)

## Verdict: KHÔNG deploy. Trả về R&D vá theo vòng audit. Không sửa logic ở cửa sổ deploy.

## Test đã chạy (tham số nạp ĐÚNG, đã verify trong report)
| Run | Symbol | Profile | Window | Bars | History | Total Trades |
|---|---|---|---|---|---|---|
| BTC AUTO | BTCUSD H4 | AutoZone=true, magic 20260724, InitBal=4600 | 2026.04.24–07.24 | 546 | 100% | **0** |
| BTC AUTO (dài) | BTCUSD H4 | như trên | 2025.01.01–2026.07.24 | 3414 | 100% | **0** |

→ Backtest R7g cho BTC sleeve có lệnh (+0.119R, PF 1.19). Live 0 lệnh suốt 18 tháng = LỖI, không phải "cửa sổ im".
→ XAU SEMI: không test headless được (cần người vẽ zone RB_HI/RB_LO) — phải chạy visual, chưa làm.

## NGUYÊN NHÂN GỐC (bằng chứng cứng, chờ R&D xác nhận)
File halt persist trong `Common\Files\`:
```
RBEA2_BTCUSD_20260724_halt.bin : init_bal=4600.00  perm_halt=1   ← BTC AUTO bị halt
RBEA2_XAUUSD_20260723_halt.bin : init_bal=4600.00  perm_halt=1   ← XAU SEMI bị halt
RBEA2_BTCUSD_20260723_halt.bin : init_bal=4600.00  perm_halt=0
```
- `HaltFile()` = `"RBEA2_"+symbol+"_"+magic+"_halt.bin"` (dòng 129) — KHÔNG có account login/instance id.
- `LoadHalt()` (OnInit, dòng 134): nếu `perm_halt==1` → `g_perm_halt=true`. `OnTick` gặp `g_perm_halt` → `return` ngay → EA đứng im, 0 lệnh.
- **Sharp-edge re-audit ĐÃ cảnh báo (ghi chú vàng i, AUDIT_G3)**: "2 account/instance cùng symbol+magic cùng máy CHIA SẺ file halt". Nay dính thật: tester (magic 20260724) + chart LIVE (magic 20260724) + chart live XAU (20260723) dùng chung file → cờ halt của phiên này áp sang phiên kia.
- Chưa truy được ai ghi perm_halt=1 đầu tiên (không có loss thực → có thể breaker tổng bắn sai ở init, HOẶC state chồng chéo giữa tester và live). CẦN R&D điều tra — cửa sổ deploy KHÔNG kết luận sâu.

## HỆ QUẢ AN TOÀN CẦN BÁO SẾP NGAY
- RB_EA đang gắn 2 chart LIVE (XAUUSD 20260723 + BTCUSD 20260724) đều có perm_halt=1 trong Common.
  → Nếu Sếp bật AlgoTrading, EA sẽ **đứng im (halt)**, KHÔNG trade. An toàn (không mất tiền) nhưng KHÔNG chạy.
- KHÔNG được xoá file halt để "chữa" rồi deploy — phải hiểu vì sao perm_halt=1 trước (Guardian: đừng
  restart/xoá state khi chưa chắc nguyên nhân).

## ĐỀ XUẤT VÁ (cho R&D v0.4, KHÔNG làm ở đây)
1. `HaltFile()` thêm account login: `"RBEA2_"+login+"_"+symbol+"_"+magic+"_halt.bin"` — tách hẳn tester/live/2 account.
2. Điều tra điều kiện set perm_halt: đảm bảo breaker tổng KHÔNG bắn khi eq≈init (0 loss). Thêm log lý do mỗi lần set perm_halt.
3. Cân nhắc: tester KHÔNG nên đọc/ghi FILE_COMMON của live (dùng FILE_COMMON có cờ MQLInfoInteger(MQL_TESTER) để tách).
4. Sau vá: G2 lại từ đầu (BTC AUTO headless + XAU SEMI visual).

## Môi trường
- File chuẩn RB_EA_v0.31.mq5 @ commit HEAD, compile G1 OK (0 err, warning 68 metadata — Sếp đã duyệt).
- Report + PNG: `<terminal>\G2_BTC_AUTO2.*`, `G2_BTC_LONG.*` trên VPS (giữ để R&D soi).
- Không sửa gì trên VPS trong phiên này (chỉ đọc + chạy tester + gắn chart theo lệnh Sếp trước đó).


---
# CẬP NHẬT CHIỀU 2026-07-24 — v0.32 VÁ XONG, G2 phần BTC AUTO **ĐẠT**
Sếp duyệt "tiến hành vá" → R&D tạo v0.32 (diff tối thiểu, tách khỏi refactor P2):
- **[H1]** HaltFile()+GV() thêm ACCOUNT_LOGIN vào khoá → tester/live/2-account hết đè nhau.
- **[H2]** Trong tester: không đọc/ghi FILE_COMMON halt (mỗi run sạch). Đã verify: chạy 2 run, Common không sinh file mới.
- **[H3]** Print lý do + eq + init_bal mỗi lần set/nạp perm_halt.
- G1 v0.32: 0 error / warning 68 (format version — cùng loại Sếp đã duyệt ở v0.31).

## Kết quả tester v0.32 (BTC AUTO, tham số thật InitBal=4600 risk 0.15, history 100%)
| Window | Trades | Net | PF | Max EqDD | Lỗ lớn nhất |
|---|---|---|---|---|---|
| 3 tháng (04→07/2026) | **7** (trước: 0) | -6.83 | 0.79 | 0.59% | -6.79 (≈0.15% ✓ đúng risk cap) |
| 18 tháng (01/25→07/26) | **13** | +48.62 | 2.24 | 0.74% | -6.99 (✓) |
- Long/short cân (8L/5S), giữ lệnh TB ~12h, không lệnh phi logic, không size bất thường.
- ⚠️ Tần suất ~0.7 lệnh/tháng — THẤP hơn nhiều tiêu chí flag(b) 10-13 lệnh/tháng: đo chính thức ở G4,
  không chặn G2 (G2 = sanity gate). Report: G2_BTC_v032*.htm (VPS + Drive INBOX_FINDINGS).

## Còn lại để đóng G2 hoàn toàn
- **XAU SEMI phải chạy VISUAL** (Sếp vẽ zone RB_HI/RB_LO) — theo G2_KIT.md, đổi Expert thành RB_EA_v0.32.
- File chuẩn MỚI: `rbea-research/RB_EA_v0.32.mq5`. v0.31 coi như deprecated (chưa gắn banner — làm khi mở file lần sau).
