# NHẬT KÝ PHIÊN — Cửa sổ R&D cloud, 24/07/2026 (sáng→đêm)
Mục đích file: cửa sổ/agent khác đọc MỘT file này là nắm được toàn bộ những gì cửa sổ R&D cloud
đã làm hôm nay, ở đâu có bằng chứng, và việc gì đang chờ ai. Chi tiết sâu → theo link từng hồ sơ.

## Dòng thời gian & sản phẩm (theo thứ tự)
1. **Đối chiếu shadow BTC** — data Bitstamp đến 24/07 01:00 UTC, 0 lệnh mới từ GOLIVE 23/07 (giá kẹt
   giữa box Donchian, đúng thiết kế). Log: `rbea-research/forward/btc_shadow_log.csv`.
2. **GOLD PROXY FEED dựng và sống** — sàn API bị sandbox chặn → đường vòng GitHub Actions cron 4h
   (`.github/workflows/gold-data.yml` trên main, Sếp duyệt) kéo XAUT-USDT (OKX/Gate) + PAXG (Binance)
   30m vào nhánh `data-gold`. Run #1 OK: 2000 nến XAUT + 1000 PAXG, 0 gap, XAUT vs PAXG lệch 0.07%.
   Fetcher: `rbea-research/forward/fetch_gold_30m.py`. Shadow proxy: `forward/xaut_shadow.py`
   (log 13 lệnh backfill, GOLIVE 24/07). LƯU Ý LESSON-04: XAUT ≠ XAUUSD CFD — chỉ là proxy tham chiếu.
3. **Spec cầu nối XAU M30 rolling-45d** giao cho agent VPS-Drive (file `XAU_M30_rolling45d.csv` →
   Drive INBOX_FINDINGS mỗi 24h) — cửa sổ deploy đã hiện thực (`forward/xau_m30_rolling45.py` + routine).
4. **EA_ENGINEERING_PLAN_v1.0.md** (Sếp duyệt) — module hóa, panel, risk kỹ thuật, debug
   "mọi quyết định tái lập được", giám sát 3 vòng, lộ trình P1-P6.
5. **P1 audit G3 (2 auditor độc lập)**: v0.3 → 4 lỗi đỏ (Đ1 Donchian shift = BTC auto 0 lệnh vĩnh viễn;
   Đ2 Friday disarm; Đ3 weekly không latch; Đ4 neo init_bal trôi) → vá **v0.31** → re-audit PASS.
   Hồ sơ: `rbea-research/AUDIT_G3_v03_20260724.md`.
6. **Đồng bộ skill đa môi trường**: `.claude/skills/` trong repo = nguồn chuẩn duy nhất (luật trong
   `hq/CLAUDE.md`); skill `deploy-vps`; janitor kiểm drift. Cửa sổ deploy bổ sung sync.sh + 6 skill.
7. **Re-audit v0.32** (bản vá halt-file của cửa sổ deploy sau G2-FAIL): H1/H2/H3 ĐẠT + 1 finding CAM
   (race login=0 khi VPS reboot) → vá **v0.33** (deferred-init, version 0.330 hết warning 68).
   **Giải xong bí ẩn perm_halt=1**: breaker bắn ĐÚNG THIẾT KẾ khi equity tụt dưới neo 4,600 do Sếp
   rút tiền/lệnh tay −763; "âm thầm" vì TG token trống. → R3 quy trình rút tiền ÁP NGAY.
   Hồ sơ: `rbea-research/AUDIT_v032_20260724.md`.
8. **FINDING KHẨN MIN-LOT** (lệnh Sếp "kiểm tra bất thường ngay"): tester 13 lệnh/PF 2.24 vs backtest
   187 lệnh — mô phỏng ngữ nghĩa EA (`rbea-research/ea_semantics_sim.py`) chứng minh flag-b chỉ cắt 15%
   (187→159); thủ phạm chính = **min-lot 0.01 nuốt ~90% tín hiệu** ở bal 4,600 risk 0.15% (BTC ATR
   median $1,089 → lot cần 0.006). XAU cũng nghẹn (min-risk $24/lệnh). PF 2.24 = artifact chọn mẫu.
   Blind spot hệ thống: mọi backtest/MC giả định lot chia vô hạn — đúng cho FTMO 200k, sai cho 4k.
   Cửa sổ deploy điều tra song song, hội tụ cùng kết luận + làm rõ Sếp trộn kỳ vọng RB_EA vs Fund EA.
   Hồ sơ: `rbea-research/FINDING_MINLOT_20260724.md`.

## Trạng thái tự động đang chạy (không cần ai)
- BTC shadow đêm 20:00 UTC (routine) · Gold feed 4h (GitHub Actions) · Janitor thứ Hai 02:00 UTC.
- XAUT shadow: chạy kèm khi có phiên (idempotent). XAU CFD: chờ feed rolling-45d từ cầu nối VPS.

## ĐANG CHỜ QUYẾT ĐỊNH / VIỆC TAY (xem STATE.md để có bản mới nhất)
- **Sếp chọn A/B/C/D cho account 3,909** (chi tiết FINDING_MINLOT mục "Lựa chọn"): RB_EA 1%/sleeve ·
  XAU-only 1% · đợi FTMO 200k · Fund EA PERSONAL (điều kiện: phải qua vòng audit 2-auditor như RB_EA).
- ⛔ CẤM cắm profile risk 0.15 lên account 3,909 (EA sẽ gần như không trade — vô nghĩa).
- Fund EA forward chết từ 15/07 (account FTMO đổi) — chờ Sếp khôi phục login.
- G2 XAU SEMI visual (Sếp vẽ zone) — chưa chạy; các version: v0.31→v0.32→v0.33→v0.4 (panel, cửa sổ deploy).
- Backlog v0.4+: R1/R2 (cash-flow/PropMode), R4 (cảnh báo min-lot OnInit), R5 (lot-quantization trong MC).

## Nguyên tắc rút ra hôm nay (đề xuất nâng thành luật)
1. Ghi chú VÀNG về state-dùng-chung phải đối xử như ĐỎ khi sắp chạm tiền (halt-file cắn sau 1 ngày).
2. Breaker không có kênh báo (TG chưa verify) = breaker nửa mù — verify kênh báo TRƯỚC khi rời máy.
3. Mọi đánh giá khả thi theo account size cụ thể PHẢI lượng tử hóa lot (R5).
