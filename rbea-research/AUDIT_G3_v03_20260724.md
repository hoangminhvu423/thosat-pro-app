# AUDIT G3 — RB_EA_v0.3.mq5 (auditor độc lập, phiên tươi, 2026-07-24)
Checklist: skill ea-code-audit. Spec đối chiếu: RB_EA_MASTER_SPEC_v1.0. Verdict: **KHÔNG ĐẠT hiện trạng → vá Đ1-Đ4 → re-audit diff**.
Bản vá: **RB_EA_v0.31.mq5** (cùng ngày, vá Đ1-Đ4 + V1/V4/V5).

## 🔴 ĐỎ (bắt buộc vá trước G1) — TẤT CẢ ĐÃ VÁ trong v0.31
- **Đ1** (dòng 185-186): AutoZone `iHighest(...,20,1)` quét bars 1..20 = GỘP CẢ NẾN TÍN HIỆU → `close > zone_hi + 0.25×ATR` bất khả thi toán học → **sleeve BTC AUTO 0 lệnh mãi mãi, chết âm thầm**. Backtest R7g dùng box 20 nến TRƯỚC nến tín hiệu. → Vá: shift 1→2 (bars 2..21).
- **Đ2** (dòng 292-297): FridayCleanup disarm vĩnh viễn, không auto re-arm → profile full-auto chết sau thứ Sáu đầu tiên (backtest không có disarm weekend, BTC 24/7). → Vá: FridayCleanup chỉ áp profile SEMI; AUTO arm sẵn lần cài đầu; /disarm tay vẫn sticky.
- **Đ3** (dòng 304 vs 313-319): breaker TUẦN không latch — day-reset "chữa lành" mỗi sáng; equity hồi từ −8.1% lên −7.9% là trade lại giữa tuần, vi phạm spec "NEUTRAL hết tuần". → Vá: cờ `g_week_halt` latch, persist GV, chỉ xóa tại Monday-reset.
- **Đ4** (dòng 268, 96, 109-121): `g_init_bal` tự-neo-lại theo balance hiện tại khi mất state (đổi VPS / đổi magic / **GlobalVariable tự xóa sau 4 tuần terminal nghỉ**) → sàn perm-halt có thể tụt DƯỚI FTMO Max Loss −10% (vd đang −4% rồi redeploy: sàn mới 86.4% initial < 90%); cờ perm-halt cũng bay hơi theo GV. → Vá: input `I_InitBalance` ($ cố định, FTMO bắt buộc set) + persist halt/init_bal ra FILE_COMMON.

## 🟡 VÀNG
- **V1** (vá luôn trong v0.31): daily `0.97×E₀` KHÔNG luôn nghiêm hơn FTMO khi ôm floating loss qua đêm >2.06% initial (chỉ xảy ra khi gap xuyên SL >5× — hiếm nhưng có). → Đổi sang FTMO-đồng-nhất: `floor = max(balance₀,equity₀) − 3%×initial` — nghiêm hơn vô điều kiện bằng cấu trúc.
- **V2** (để G4 đo): time-stop giờ-treo-tường vs backtest đếm-nến — XAU ôm qua weekend bị cắt sớm ~12 nến H4; BTC 24/7 khớp.
- **V3** (verify trên FTMO): I_Deviation=30 points quá hẹp cho BTC nếu instant-execution (market-execution thì vô hại).
- **V4** (vá luôn): default risk 0.25 → 0.15 theo spec khởi đầu.
- **V5** (vá luôn): PERM-HALT giờ vẫn retry đóng vị thế mỗi 5' nếu lần đầu fail.
- **V6** (đối chiếu backtest trước khi đổi): whipsaw-block chỉ chặn RANGE, không chặn BREAK cùng hướng — backtest Python cũng KHÔNG có whipsaw cho break sleeve → giữ nguyên là ĐÚNG fidelity.
- **V7** (chấp nhận ở H4): TG/WebRequest blocking 3s trong luồng chính.

## 🟢 XANH (checklist pass)
Filling-mode auto + close verified retry ×3 · chống repaint (index 1 + new-bar gating + CopyBuffer shift 1) · handle 1 lần OnInit/release OnDeinit · sizing không ép min-lot, guard chia-0 · F2 budget-on-fill · F4 stops clamp · Monday week-reset đúng C7 · TG whitelist 1 chat.

## PHÁN QUYẾT 3 FLAG HEADER
- **(a) Market-immediate khi giá vượt trigger: KHÔNG phải lỗi — đúng spec C4-fix**, backtest cũng fill tại close nến xác nhận; SL/TP neo giá fill thực nên hình học 1R/2R bảo toàn; lệch còn lại (spread+gap) nằm trong fee-stress ×2, đo tiếp G4.
- **(b) 1-vị-thế vs backtest overlap: lệch CÓ THẬT** (OursOpen dòng 341/348 + budget + gap60 + false-break-lock). Mẫu lệnh live không phải mẫu ngẫu nhiên của backtest → +0.119R không chuyển thẳng. Không vá code — đo ở G4 (tiêu chí 10-13 lệnh/tháng ±40%).
- **(c) Daily formula: đạt-có-điều-kiện** → đã nâng cấp thành vô-điều-kiện trong v0.31 (V1).

## TRẠNG THÁI SAU VÁ
v0.31 chờ **re-audit diff** (phiên tươi, chỉ soi phần vá) → nếu sạch: G3 ĐẠT có điều kiện, kèm điều kiện G4: đo lệch tần suất flag (b) + quan sát ít nhất 1 lần daily/weekly breaker hoạt động đúng latch.

---
## RE-AUDIT DIFF v0.31 (auditor độc lập thứ 2, cùng ngày) — VERDICT: **ĐẠT G3, có điều kiện G4**
- Đ1 shift 2..21 khớp semantic backtest, không off-by-one ✓ · Đ2 fresh-flag đúng thứ tự, /disarm sticky ✓
- Đ3 latch tuần persist + gate đặt SAU time-stop/breaker tổng (2 cái này vẫn chạy khi halt) ✓
- Đ4 input override vô điều kiện, FILE_COMMON đúng cú pháp, sống sót kịch bản GV bay hơi 5 tuần ✓
- V1 nghiêm hơn FTMO bằng cấu trúc trong mọi trạng thái ✓ · V5 static per-chart-instance, throttle 5' ✓
- Regression: SẠCH (20 GV key cân xứng, không TG spam, cơ chế xanh không bị chạm).
- 2 ghi chú VÀNG cho version sau (không chặn): (i) tên halt-file nên thêm account login (2 account cùng
  symbol+magic cùng máy sẽ chia sẻ file — hướng fail an toàn); (ii) nếu đè GV cũ v0.3 thì AUTO cần /arm tay 1 lần.
- Điều kiện G4 giữ nguyên: G1+G2 trước demo · đo lệch tần suất flag (b) · quan sát ≥1 lần breaker latch đúng.
**FILE CHUẨN ĐỂ COMPILE (G1): RB_EA_v0.31.mq5**
