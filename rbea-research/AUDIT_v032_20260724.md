# RE-AUDIT v0.32 + ĐIỀU TRA perm_halt (auditor độc lập, 2026-07-24 chiều)
## Verdict diff v0.32: H1/H2/H3 ĐẠT, regression sạch (Đ1-Đ4, V1, V5 nguyên vẹn), KÈM 1 finding CAM:
- **CAM — race login=0**: OnInit chạy khi terminal chưa kết nối (VPS reboot + autologon = đúng kịch bản
  RUNBOOK A đang bật) → ACCOUNT_LOGIN=0 → mọi khóa thành RBEA2_0_... → perm_halt bị "quên" + BTC auto
  tự re-arm với khóa sai. → **ĐÃ VÁ trong v0.33 [L1]: deferred init** — login=0 thì hoãn nạp state tới
  tick đầu (tick chỉ đến khi đã kết nối); guard OnTimer/OnDeinit/OnTradeTransaction trong lúc chờ.
- Vàng (vá luôn v0.33): [L2] version "0.330" (hết warning 68) + chuỗi nội bộ v0.33 + TG online kèm login;
  [L3] log khi nạp perm_halt từ GV. Vàng để v0.4: tg_offset replay sau đổi khóa; migration khóa state.

## Điều tra perm_halt=1 (G2-FAIL sáng): GIẢ THUYẾT ĐỨNG — breaker bắn ĐÚNG THIẾT KẾ
Chuỗi: 2 chart live gắn v0.31 neo 4,600 → trong ngày equity tụt 4,673→~3,910 (Sếp rút tiền/lệnh tay
−763) → dưới sàn 4,186 (−9%) → breaker tổng ghi perm_halt=1 vào 2 file Common (OnTick chạy cả khi
AlgoTrading OFF) → "âm thầm" vì TG token trống → tester cùng magic đọc phải → 0 lệnh.
Bằng chứng loại trừ: file XAU=1 dù XAU chưa từng chạy tester; file BTCUSD_20260723 (magic default,
không gắn live đúng tổ hợp)=0. Code chỉ có MỘT đường flip 0→1 (breaker OnTick).
**Kết luận thiết kế: EA không phân biệt RÚT TIỀN với THUA LỖ.** Đúng cho FTMO, sai cho account cá nhân.

## Yêu cầu sinh ra
- **R1 (v0.4)**: bắt DEAL_TYPE_BALANCE → dịch neo g_init_bal theo nạp/rút + TG thông báo.
- **R2 (v0.4)**: input I_PropMode — prop = neo cứng (luật FTMO), personal = neo dịch cash-flow.
- **R3 (ÁP NGAY, thủ công)**: quy trình rút tiền = disarm → rút → chỉnh I_InitBalance theo số dư mới
  → xóa halt file+GV CÓ CHỦ ĐÍCH (ghi DECISION_LOG) → re-arm → verify TG.

## Điều kiện chạm tiền thật (sau khi XAU SEMI visual đạt)
1. Dùng **v0.33** (đã vá race login) → G1 recompile (kỳ vọng 0/0 vì L2) + G2 BTC headless lại (~15').
2. I_InitBalance = số dư ngày cắm (~3,909 — verify lại lúc attach).
3. Dọn tàn dư v0.31: archive 3 file RBEA2_*_halt.bin cũ + GV khóa cũ, ghi DECISION_LOG (được phép —
   nguyên nhân đã hiểu, Guardian thỏa).
4. **Telegram bot riêng hoạt động + verify trước khi rời máy** — sự cố hôm nay "âm thầm" chỉ vì token trống.
5. Áp R3 cho mọi lần rút tiền kể từ nay.
